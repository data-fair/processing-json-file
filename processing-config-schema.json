{
  "type": "object",
  "x-display": "tabs",
  "required": ["dataset", "url", "format"],
  "allOf": [
    {
      "title": "Jeu de données",
      "properties": {
        "dataset": {
          "type": "object",
          "title": "Jeu de données (éditable uniquement)",
          "x-fromUrl": "{context.dataFairUrl}/api/v1/datasets?q={q}&select=id,title&rest=true&{context.ownerFilter}",
          "x-itemsProp": "results",
          "x-itemTitle": "title",
          "x-itemKey": "id",
          "properties": {
            "id": { "type": "string", "title": "Identifiant" },
            "title": { "type": "string", "title": "Titre" }
          }
        },
        "drop": {
          "type": "boolean",
          "title": "Supprimer les données avant import",
          "description": "Supprime toutes les lignes du jeu de données éditable avant d'y importer les nouvelles. Si décoché, les lignes seront ajoutées à la suite / éditées selon la clé primaire du jeu de données.",
          "default": false
        }
      }
    },
    {
      "title": "Source des données",
      "properties": {
        "url": {
          "title": "URL source",
          "type": "string",
          "description": "Les protocoles supportés sont HTTP, HTTPS, FTP et SFTP.\n\n- **URL vers un dossier** (se terminant par `/`) : importe tous les fichiers correspondant au format choisi (onglet « Champs à récupérer ») dans le dossier (extension insensible à la casse, `.json` comme `.JSON`, `.csv` comme `.CSV`).\n- **URL vers un fichier** (se terminant par `.json` ou `.csv` selon le format choisi) : importe ce fichier uniquement.\n\nSi l'URL ne se termine ni par `/` ni par l'extension attendue, le comportement par défaut tente de télécharger un fichier unique."
        },
        "username": {
          "title": "Utilisateur",
          "type": "string",
          "layout": {
            "props": {
              "autocomplete": "suppress"
            }
          }
        },
        "password": {
          "title": "Mot de passe",
          "type": "string",
          "layout": {
            "props": {
              "type": "password",
              "autocomplete": "suppress"
            }
          }
        },
        "processAndDelete": {
          "type": "boolean",
          "title": "Supprimer le(s) fichier(s) source après import (FTP/SFTP uniquement)",
          "description": "Disponible uniquement pour les sources FTP et SFTP.\n\n- Après un import réussi, chaque fichier source téléchargé est supprimé du serveur distant (ou déplacé, voir l'option « Déplacer au lieu de supprimer »).\n- Si aucun fichier n'est trouvé sur le serveur, l'exécution est ignorée : elle se termine sans erreur et n'apparaît pas dans la liste des exécutions."
        }
      },
      "dependencies": {
        "processAndDelete": {
          "properties": {
            "moveInsteadOfDelete": {
              "type": "boolean",
              "title": "Déplacer au lieu de supprimer",
              "description": "Au lieu de supprimer le(s) fichier(s) source après import, les déplace dans un sous-dossier `backup` : créé à côté du fichier si l'URL source pointe vers un fichier unique, ou à l'intérieur du dossier importé si l'URL source pointe vers un dossier."
            }
          }
        }
      }
    },
    {
      "title": "Champs à récupérer",
      "oneOf": [
        {
          "title": "JSON",
          "properties": {
            "format": {
              "type": "string",
              "const": "json",
              "title": "Format des fichiers sources",
              "default": "json"
            },
            "resultsPath": {
              "type": "string",
              "title": "Chemin des résultats",
              "description": "Chemin d'accès du tableau de résultats dans le json",
              "default": ""
            },
            "block": { "$ref": "#/definitions/block" },
            "separator": {
              "type": "string",
              "title": "Séparateur pour les tableaux",
              "default": ";"
            }
          }
        },
        {
          "title": "CSV",
          "description": "Les fichiers CSV sont chargés tels quels : les colonnes du fichier deviennent les colonnes du jeu de données, sans configuration supplémentaire.",
          "properties": {
            "format": {
              "type": "string",
              "const": "csv",
              "title": "Format des fichiers sources"
            }
          }
        }
      ]
    }
  ],
  "definitions": {
    "block": {
      "type": "object",
      "properties": {
        "mapping": {
          "type": "array",
          "title": "Champs à récupérer",
          "description": "Les colonnes qui seront récupérées depuis ce niveau",
          "x-itemTitle": "key",
          "items": {
            "type": "object",
            "required": ["key", "path"],
            "properties": {
              "key": {
                "type": "string",
                "title": "Identifiant de la colonne",
                "description": "Clé de la colonne"
              },
              "path": {
                "type": "string",
                "title": "Chemin de la colonne",
                "description": "Chemin d'accès dans le json à partir de cette position"
              }
            }
          }
        },
        "expand": {
          "title": "Données en profondeur d'un tableau",
          "type": "object",
          "layout": "card",
          "properties": {
            "path": {
              "type": "string",
              "title": "Chemin de la colonne",
              "description": "Chemin d'accès dans le json à partir de cette position"
            }
          },
          "dependencies": {
            "path": {
              "properties": {
                "block": {
                  "$ref": "#/definitions/block"
                }
              }
            }
          }
        }
      }
    }
  }
}
